<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.examen1b">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Examen1B">
        <activity android:name=".vista_actualizar_libro"></activity>
        <activity android:name=".vista_crear_libro" />
        <activity android:name=".vista_actualizar_autor" />
        <activity android:name=".vista_crear_autor" />
        <activity android:name=".vista_autor" />
        <activity android:name=".lista_autores" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>